@using ForumNet.Web.Infrastructure.Extensions
@using ForumNet.Web.ViewModels.Posts
@using ForumNet.Web.ViewModels.Replies
@model PostsDetailsViewModel
@{
    ViewData["Title"] = Model.Title;
}

<br />
<div class="container">
    <div class="tt-single-topic-list">
        <div class="tt-item">
            <div class="tt-single-topic">
                <div class="tt-item-header">
                    <div class="tt-item-info info-top">
                        <div class="tt-avatar-icon">
                            <i class="tt-icon"><svg><use xlink:href="#@Model.Author.ProfilePicture"></use></svg></i>
                        </div>
                        <div class="tt-avatar-title">
                            <a href="#">@Model.Author.UserName</a>
                        </div>
                        <div class="tt-info-time">
                            @if (this.User.IsAdministrator())
                            {
                                <button style="padding: 0; border: none; background: none; outline: none; color: #666f74" onclick="pinPost(@Model.Id)">
                                    <i class="fas fa-thumbtack"></i>
                                    <span id="isPinned">@(Model.IsPinned ? "Unpin" : "Pin")</span>
                                </button>
                            }
                            <i class="tt-icon"><svg><use xlink:href="#icon-time"></use></svg></i>@Model.CreatedOn
                        </div>
                    </div>
                    <h3 class="tt-item-title">
                        <a href="#">@Model.Title</a>
                    </h3>
                    <div class="tt-item-tag">
                        <ul class="tt-list-badge">
                            <li>
                                <a asp-area="" asp-controller="Categories" asp-action="Details" asp-route-id="@Model.Category.Id">
                                    <span class="tt-color03 tt-badge">@Model.Category.Name</span>
                                </a>
                            </li>
                            @foreach (var tag in Model.Tags)
                            {
                                <li><a href="#"><span class="tt-badge">@tag.Name</span></a></li>
                            }
                        </ul>
                    </div>
                </div>
                <div class="tt-item-description">
                    <p>@Html.Raw(Model.SanitizedDescription)</p>
                </div>
                <div class="tt-item-info info-bottom">
                    <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="likeReaction(@Model.Id, 'post-reactions', 'post')">
                        <i class="tt-icon"><svg><use xlink:href="#icon-like"></use></svg></i>
                    </button>
                    <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="loveReaction(@Model.Id, 'post-reactions', 'post')">
                        <i class="fas fa-heart"></i>
                    </button>
                    <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="hahaReaction(@Model.Id, 'post-reactions', 'post')">
                        <i class="fas fa-grin-tears"></i>
                    </button>
                    <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="wowReaction(@Model.Id, 'post-reactions', 'post')">
                        <i class="fas fa-surprise"></i>
                    </button>
                    <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="sadReaction(@Model.Id, 'post-reactions', 'post')">
                        <i class="fas fa-sad-tear"></i>
                    </button>
                    <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="angryReaction(@Model.Id, 'post-reactions', 'post')">
                        <i class="fas fa-angry"></i>
                    </button>
                    <div class="col-separator"></div>
                    @if (this.Model.Author.Id == this.Model.CurrentUserId || this.User.IsAdministrator())
                    {
                        <a asp-area="" asp-controller="Posts" asp-action="Edit" asp-route-id="@Model.Id" class="tt-icon-btn" style="color: #666f74">
                            <i class="fas fa-pen"></i>
                        </a>
                        <a asp-area="" asp-controller="Posts" asp-action="Delete" asp-route-id="@Model.Id" class="tt-icon-btn" style="color: #666f74">
                            <i class="fas fa-trash-alt"></i>
                        </a>
                    }
                    <a href="#" class="tt-icon-btn tt-hover-02 tt-small-indent">
                        <i class="tt-icon"><svg><use xlink:href="#icon-share"></use></svg></i>
                    </a>
                    <a href="#" class="tt-icon-btn tt-hover-02 tt-small-indent">
                        <i class="tt-icon"><svg><use xlink:href="#icon-flag"></use></svg></i>
                    </a>
                    <button style="padding: 0; border:none; background:none; outline:none" class="tt-icon-btn tt-hover-02 tt-small-indent" onclick="animateButton()">
                        <i class="tt-icon"><svg><use xlink:href="#icon-reply"></use></svg></i>
                    </button>
                </div>
            </div>
        </div>
        <partial name="~/Views/Posts/_DetailsThreadStatusPartial.cshtml" model="@Model" />
        @foreach (var reply in Model.Replies)
        {
            <div class="tt-item">
                <div class="tt-single-topic">
                    <div class="tt-item-header pt-noborder">
                        <div class="tt-item-info info-top">
                            <div class="tt-avatar-icon">
                                <i class="tt-icon"><svg><use xlink:href="#@reply.Author.ProfilePicture"></use></svg></i>
                            </div>
                            <div class="tt-avatar-title">
                                <a href="#">@reply.Author.UserName</a>
                            </div>
                            <a href="#" class="tt-info-time">
                                <i class="tt-icon"><svg><use xlink:href="#icon-time"></use></svg></i>@reply.CreatedOn
                            </a>
                        </div>
                    </div>
                    <div class="tt-item-description">
                        <p>@Html.Raw(reply.SanitizedDescription)</p>
                    </div>
                    <div class="tt-item-info info-bottom">
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="likeReaction(@reply.Id, 'reply-reactions', 'reply')">
                            <i class="tt-icon"><svg><use xlink:href="#icon-like"></use></svg></i>
                        </button>
                        &nbsp;
                        <span id="reply-likes-@reply.Id" class="tt-text">@reply.Likes</span>
                        &nbsp;
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="loveReaction(@reply.Id, 'reply-reactions', 'reply')">
                            <i class="fas fa-heart"></i>
                        </button>
                        &nbsp;
                        <span id="reply-loves-@reply.Id" class="tt-text">@reply.Loves</span>
                        &nbsp;
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="hahaReaction(@reply.Id, 'reply-reactions', 'reply')">
                            <i class="fas fa-grin-tears"></i>
                        </button>
                        &nbsp;
                        <span id="reply-haha-@reply.Id" class="tt-text">@reply.HahaCount</span>
                        &nbsp;
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="wowReaction(@reply.Id, 'reply-reactions', 'reply')">
                            <i class="fas fa-surprise"></i>
                        </button>
                        &nbsp;
                        <span id="reply-wow-@reply.Id" class="tt-text">@reply.WowCount</span>
                        &nbsp;
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="sadReaction(@reply.Id, 'reply-reactions', 'reply')">
                            <i class="fas fa-sad-tear"></i>
                        </button>
                        &nbsp;
                        <span id="reply-sad-@reply.Id" class="tt-text">@reply.SadCount</span>
                        &nbsp;
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74" onclick="angryReaction(@reply.Id, 'reply-reactions', 'reply')">
                            <i class="fas fa-angry"></i>
                        </button>
                        &nbsp;
                        <span id="reply-angry-@reply.Id" class="tt-text">@reply.AngryCount</span>
                        &nbsp;
                        <a asp-area="" asp-controller="Replies" asp-action="Details" asp-route-id="@reply.Id" class="tt-icon-btn">
                            <i class="tt-icon"><svg><use xlink:href="#icon-view"></use></svg></i>
                        </a>
                        <div class="col-separator"></div>
                        @if (reply.Author.Id == this.Model.CurrentUserId || this.User.IsAdministrator())
                        {
                            <a asp-area="" asp-controller="Replies" asp-action="Edit" asp-route-id="@reply.Id" class="tt-icon-btn" style="color: #666f74">
                                <i class="fas fa-pen"></i>
                            </a>
                            <a asp-area="" asp-controller="Replies" asp-action="Delete" asp-route-id="@reply.Id" class="tt-icon-btn" style="color: #666f74">
                                <i class="fas fa-trash-alt"></i>
                            </a>
                        }
                        <a href="#" class="tt-icon-btn tt-hover-02 tt-small-indent">
                            <i class="tt-icon"><svg><use xlink:href="#icon-share"></use></svg></i>
                        </a>
                        <a href="#" class="tt-icon-btn tt-hover-02 tt-small-indent">
                            <i class="tt-icon"><svg><use xlink:href="#icon-flag"></use></svg></i>
                        </a>
                        <button type="button" class="tt-icon-btn" style="padding: 0; border:none; background:none; outline: none; color: #666f74">
                            <i class="tt-icon"><svg><use xlink:href="#icon-reply"></use></svg></i>
                        </button>
                    </div>
                </div>
            </div>
        }
    </div>
    @if (Model.Replies.Any())
    {
        <div class="tt-wrapper-inner">
            <h4 class="tt-title-separator"><span>You’ve reached the end of replies</span></h4>
        </div>
    }
    <div id="reply-box">
        <partial name="~/Views/Replies/_CreatePartial.cshtml" model="new RepliesCreateInputModel { PostId = Model.Id }" />
    </div>
</div>

@section Scripts {
    <script>
        tinymce.init({
            selector: '#reply-create',
            height: 300,
            plugins: [
                'lists advlist link autolink image media preview hr anchor',
                'code codesample paste',
                'table emoticons help'
            ],
            toolbar: [
                'undo redo | styleselect fontsizeselect | bold italic | alignleft aligncenter alignright alignjustify | outdent indent bullist numlist |',
                'forecolor backcolor emoticons | link image media codesample preview | help'
            ],
            link_default_protocol: "https",
            menubar: 'file edit view insert format table'
        });
    </script>
    <script>
        function animateButton() {
            $('html, body').animate({
                scrollTop: $("#reply-box").offset().top
            }, 1500);
        }

        function pinPost(id) {
            $.ajax({
                type: "POST",
                url: "/Administration/Posts/Pin/" + id,
                success: function (json) {
                    $('#isPinned').html(json.pinOrUnpin);
                }
            });
        }

        function likeReaction(id, controller, type) {
            $.ajax({
                type: "POST",
                url: "/api/" + controller + "/" + id + "/like",
                success: function (json) {
                    if (type === 'post') {
                        $("#post-likes").html(json.likes);
                        $("#post-loves").html(json.loves);
                        $("#post-haha").html(json.haha);
                        $("#post-wow").html(json.wow);
                        $("#post-sad").html(json.sad);
                        $("#post-angry").html(json.angry);
                    } else {
                        $("#reply-likes-" + id).html(json.likes);
                        $("#reply-loves-" + id).html(json.loves);
                        $("#reply-haha-" + id).html(json.haha);
                        $("#reply-wow-" + id).html(json.wow);
                        $("#reply-sad-" + id).html(json.sad);
                        $("#reply-angry-" + id).html(json.angry);
                    }
                }
            });
        }

        function loveReaction(id, controller, type) {
            $.ajax({
                type: "POST",
                url: "/api/" + controller + "/" + id + "/love",
                success: function (json) {
                    if (type === 'post') {
                        $("#post-likes").html(json.likes);
                        $("#post-loves").html(json.loves);
                        $("#post-haha").html(json.haha);
                        $("#post-wow").html(json.wow);
                        $("#post-sad").html(json.sad);
                        $("#post-angry").html(json.angry);
                    } else {
                        $("#reply-likes-" + id).html(json.likes);
                        $("#reply-loves-" + id).html(json.loves);
                        $("#reply-haha-" + id).html(json.haha);
                        $("#reply-wow-" + id).html(json.wow);
                        $("#reply-sad-" + id).html(json.sad);
                        $("#reply-angry-" + id).html(json.angry);
                    }
                }
            });
        }

        function hahaReaction(id, controller, type) {
            $.ajax({
                type: "POST",
                url: "/api/" + controller + "/" + id + "/haha",
                success: function (json) {
                    if (type === 'post') {
                        $("#post-likes").html(json.likes);
                        $("#post-loves").html(json.loves);
                        $("#post-haha").html(json.haha);
                        $("#post-wow").html(json.wow);
                        $("#post-sad").html(json.sad);
                        $("#post-angry").html(json.angry);
                    } else {
                        $("#reply-likes-" + id).html(json.likes);
                        $("#reply-loves-" + id).html(json.loves);
                        $("#reply-haha-" + id).html(json.haha);
                        $("#reply-wow-" + id).html(json.wow);
                        $("#reply-sad-" + id).html(json.sad);
                        $("#reply-angry-" + id).html(json.angry);
                    }
                }
            });
        }

        function wowReaction(id, controller, type) {
            $.ajax({
                type: "POST",
                url: "/api/" + controller + "/" + id + "/wow",
                success: function (json) {
                    if (type === 'post') {
                        $("#post-likes").html(json.likes);
                        $("#post-loves").html(json.loves);
                        $("#post-haha").html(json.haha);
                        $("#post-wow").html(json.wow);
                        $("#post-sad").html(json.sad);
                        $("#post-angry").html(json.angry);
                    } else {
                        $("#reply-likes-" + id).html(json.likes);
                        $("#reply-loves-" + id).html(json.loves);
                        $("#reply-haha-" + id).html(json.haha);
                        $("#reply-wow-" + id).html(json.wow);
                        $("#reply-sad-" + id).html(json.sad);
                        $("#reply-angry-" + id).html(json.angry);
                    }
                }
            });
        }

        function sadReaction(id, controller, type) {
            $.ajax({
                type: "POST",
                url: "/api/" + controller + "/" + id + "/sad",
                success: function (json) {
                    if (type === 'post') {
                        $("#post-likes").html(json.likes);
                        $("#post-loves").html(json.loves);
                        $("#post-haha").html(json.haha);
                        $("#post-wow").html(json.wow);
                        $("#post-sad").html(json.sad);
                        $("#post-angry").html(json.angry);
                    } else {
                        $("#reply-likes-" + id).html(json.likes);
                        $("#reply-loves-" + id).html(json.loves);
                        $("#reply-haha-" + id).html(json.haha);
                        $("#reply-wow-" + id).html(json.wow);
                        $("#reply-sad-" + id).html(json.sad);
                        $("#reply-angry-" + id).html(json.angry);
                    }
                }
            });
        }

        function angryReaction(id, controller, type) {
            $.ajax({
                type: "POST",
                url: "/api/" + controller + "/" + id + "/angry",
                success: function (json) {
                    if (type === 'post') {
                        $("#post-likes").html(json.likes);
                        $("#post-loves").html(json.loves);
                        $("#post-haha").html(json.haha);
                        $("#post-wow").html(json.wow);
                        $("#post-sad").html(json.sad);
                        $("#post-angry").html(json.angry);
                    } else {
                        $("#reply-likes-" + id).html(json.likes);
                        $("#reply-loves-" + id).html(json.loves);
                        $("#reply-haha-" + id).html(json.haha);
                        $("#reply-wow-" + id).html(json.wow);
                        $("#reply-sad-" + id).html(json.sad);
                        $("#reply-angry-" + id).html(json.angry);
                    }
                }
            });
        }
    </script>
    <partial name="_ValidationScriptsPartial" />
}