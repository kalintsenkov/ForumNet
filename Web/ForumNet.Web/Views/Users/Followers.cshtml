@using ForumNet.Web.Infrastructure.Extensions
@model ForumNet.Web.ViewModels.Users.UsersFollowersViewModel
@{
    ViewData["Title"] = Model.UserName;
}

<div class="tt-wrapper-section">
    <div class="container">
        <div class="tt-user-header">
            <div class="tt-col-avatar">
                <div class="tt-icon">
                    <svg class="tt-icon">
                        <use xlink:href="#@Model.ProfilePicture"></use>
                    </svg>
                </div>
            </div>
            <div class="tt-col-title">
                <div class="tt-title">
                    <a asp-area="" asp-controller="Users" asp-action="Threads" asp-route-id="@Model.Id">@Model.UserName</a>
                </div>
                <ul class="tt-list-badge">
                    <li><a href="#"><span class="tt-color14 tt-badge">LVL : @Model.Level</span></a></li>
                </ul>
            </div>
            @if (Model.Id != this.User.GetId())
            {
                <div class="tt-col-btn">
                    <div class="tt-list-btn">
                        <a href="#" class="btn btn-primary">Message</a>
                        <a id="follow-button" href="#" class="btn btn-secondary" onclick="follow('@Model.Id')">Follow</a>
                    </div>
                </div>
            }
        </div>
    </div>
</div>
<div class="container">
    <div class="tt-tab-wrapper">
        <div class="tt-wrapper-inner">
            <ul class="nav nav-tabs pt-tabs-default" role="tablist">
                <li class="nav-item show">
                    <a class="nav-link" asp-area="" asp-controller="Users" asp-action="Threads" asp-route-id="@Model.Id">
                        <span>Threads</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" asp-area="" asp-controller="Users" asp-action="Replies" asp-route-id="@Model.Id">
                        <span>Replies</span>
                    </a>
                </li>
                <li class="nav-item tt-hide-xs show">
                    <a class="nav-link active" asp-area="" asp-controller="Users" asp-action="Followers" asp-route-id="@Model.Id">
                        <span>@Model.FollowersCount Followers</span>
                    </a>
                </li>
                <li class="nav-item tt-hide-md">
                    <a class="nav-link" asp-area="" asp-controller="Users" asp-action="Following" asp-route-id="@Model.Id">
                        <span>@Model.FollowingCount Following</span>
                    </a>
                </li>
            </ul>
        </div>
        <div class="tab-pane tt-indent-none show active" id="tt-tab-04" role="tabpanel">
            @if (Model.Followers.Any())
            {
                foreach (var follower in Model.Followers)
                {
                    <div class="tt-followers-list">
                        <div class="tt-list-header">
                            <div class="tt-col-name">User</div>
                            <div class="tt-col-value-large hide-mobile">Threads</div>
                            <div class="tt-col-value-large hide-mobile">Replies</div>
                            <div class="tt-col-value">Level</div>
                        </div>
                        <div class="tt-item">
                            <div class="tt-col-merged">
                                <div class="tt-col-avatar">
                                    <svg class="tt-icon">
                                        <use xlink:href="#@follower.ProfilePicture"></use>
                                    </svg>
                                </div>
                                <div class="tt-col-description">
                                    <h6 class="tt-title">
                                        <a asp-area="" asp-controller="Users" asp-action="Threads" asp-route-id="@follower.Id">
                                            @follower.UserName
                                        </a>
                                    </h6>
                                </div>
                            </div>
                            <div class="tt-col-value-large hide-mobile">@follower.ThreadsCount</div>
                            <div class="tt-col-value-large hide-mobile">@follower.RepliesCount</div>
                            <div class="tt-col-value"><span class="tt-color16 tt-badge">LVL : @follower.Level</span></div>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="tt-layout-404">
                    <h6 class="tt-title">@Model.UserName has no followers.</h6>
                </div>
            }
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function follow(id) {
            $.ajax({
                type: "POST",
                url: "/Users/Follow/" + id,
                success: function (json) {
                    if (json == true) {
                        $('#follow-button').html('Following');
                    } else {
                        $('#follow-button').html('Follow');
                    }
                }
            });
        }
    </script>
}
